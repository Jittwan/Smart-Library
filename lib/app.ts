// Public app name — inlined at build time via NEXT_PUBLIC_ prefix so it can be
// used in both Server and Client Components.
export const APP_NAME = process.env.NEXT_PUBLIC_APP_NAME || "Smart Library";
